# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Service role key (only for backend API server, NOT for extension)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# OpenAI API Key (stored in chrome.storage, not in .env)
# Users will configure this via the extension settings UI
